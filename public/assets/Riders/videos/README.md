# Riders header footage

The header currently uses `/public/rider-video.mp4`.
The playlist is configured in `src/components/ui/RidersHeroVideo.tsx`.
It plays muted and loops, with a pause control and a still-image fallback
for playback failure or reduced-motion preferences.
